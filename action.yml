name: "Quokka MAST"
description: "Prevent zero-day attacks with the only mobile security intelligence with contextual awareness. Scan your applications on Quokka's Mobile Application Secuirty Testing Platform."
inputs:
  path-to-file:
    description: "Path to the artifact file"
    required: true
  platform:
    description: "Platform the app belongs to"
    required: true
  apiKey:
    description: "API Key for the user"
    required: true
runs:
  using: "node12"
  main: "index.js"
branding:
  icon: "award"
  color: "green"
